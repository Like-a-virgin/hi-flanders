<div class="box">
    <div class="box__heading">
        <h1>{{'Totaal'|t}}</h1>
    </div>
    <div class="box__content">
        <div class="box__column box__column--2">            
            <div class="box__info">
                <p>{{ user.memberRate.one().title }}</p>
                <p>{{ user.memberRate.one().price|money }}</p>
            </div>
        </div>
        <div class="box__column box__column--2">            
            <p>Totaal:</p>
            <p>{{ user.memberRate.one().price|money }}</p>
        </div>

    </div>
</div>