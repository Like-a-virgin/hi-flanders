{% extends "layouts/base.twig" %}
    
{% requireLogin %}

{% block content %}
    {# {% if craft.app.user.identity and craft.app.user.identity.admin %}
        {% redirect url('admin') %}
    {% else %} #}
        {% if currentUser.inGroup('members') or currentUser.inGroup('membersGroup') %}    
            {% include "pages/user.twig" %} 
        {% elseif currentUser.inGroup('membersAdmin') or currentUser.inGroup('membersAdminSuper') or (craft.app.user.identity and craft.app.user.identity.admin) %}
            {{ sprig('sprig-components/dashboard.twig')}}
        {% endif %}

    {# {% endif %} #}
{% endblock %}